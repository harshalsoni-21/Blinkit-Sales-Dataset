# Blinkit-Sales-Dataset
Blinkit analytics project in two parts: a Jupyter notebook running full order-lifecycle EDA across 5,000 orders (cleaning, merging, sales &amp; delivery insights), plus a standalone Chart.js dashboard exploring customer segments, spend, and geography — built on a messier extract, with every proxy spend figure clearly flagged.
